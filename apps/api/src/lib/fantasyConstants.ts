/**
 * Shared fantasy timing constants (winter division rosters + tournament multi-event).
 * Single source of truth for “how long before first match edits freeze.”
 */
export const ROSTER_EDIT_LOCK_MS = 60 * 60 * 1000;
