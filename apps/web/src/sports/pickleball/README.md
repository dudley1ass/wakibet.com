# Pickleball Module

Phase 1 module setup for pickleball UI.

This namespace currently re-exports existing pickleball dashboard and roster/pick pages.
It allows gradual migration to `src/sports/pickleball/*` without breaking routes.
