# Copy to `.env` next to this file. Expo injects `EXPO_PUBLIC_*` at bundle time.

# LAN IP of your dev machine when testing on a physical device (not 127.0.0.1).
EXPO_PUBLIC_API_URL=http://127.0.0.1:3000
