/**
 * OpenAPI-derived types for the WakiBet HTTP API.
 * Regenerate from the repo root: `pnpm generate:api-types`
 * (requires a successful `pnpm install` and writes `openapi.json`).
 */
export interface paths {}

export type webhooks = Record<string, never>;

export interface components {}

export interface operations {}

export type external = Record<string, never>;
