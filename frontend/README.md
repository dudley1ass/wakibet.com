This folder exists so a Render **Static Site** can keep **Root Directory = `frontend`** (from the old repo layout) and still build the current Vite app in `apps/web`.

Prefer updating Render to **empty root** + publish `apps/web/dist` when you can.
